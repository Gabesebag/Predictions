function titleScreen() {
//    if (currentScreen === 0) {
    fill('white');
    textSize(32);
    textAlign(CENTER, CENTER);
    text("Welcome to the Prediction Game!", width / 2, height / 2 - 40);
    textSize(20);
    text("Click to start", width / 2, height / 2 + 20);
//    }
}